{
  "name": "hertzblatt",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build --public-url ./ src/index.html && mkdir -p dist_single && inliner dist/index.html >dist_single/index.html"
  },
  "devDependencies": {
    "inliner": "^1.13.1",
    "parcel": "^2.8.2"
  }
}
